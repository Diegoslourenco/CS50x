#include "helpers.h"
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(void)
{
    int tmp[3][3];
    tmp[3][3] = 1

    int soma  = tmp[3][3] + tmp[-1][-1];

    int n = 255;
    n = round((float)sqrt((float)(25 + pow(n, 2))));
    printf("%i\n", soma);
}